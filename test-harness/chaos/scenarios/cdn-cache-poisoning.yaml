# CHAOS-EDGE-03 — Stale 500 asset pinned with max-age=86400; invalidation must drain inside 5 min
# Source: chaos/SCENARIOS.md catalog
# Cites: D31 (SLO), D32 (auto-runbook), D37 (TDD layers)
# Tags: nightly

id: CHAOS-EDGE-03
name: cdn-stale-content
tier: edge
executor: native_gcp
tags: [nightly]

description: |
  Stale 500 asset pinned with max-age=86400; invalidation must drain inside 5 min.

expected_rto_s: 300
expected_rpo_s: 0
observed_sli_breach: slo.cdn.cache_hit_ratio.regression

auto_runbook: runbook-cdn-invalidate
auto_runbook_timeout_s: 60

recovery_validation:
  type: probe
  steps:
    - "see chaos/SCENARIOS.md recovery_validation block for CHAOS-EDGE-03"

pass_criteria:
  - "observed_rto_s <= 300"
  - "observed_rpo_s <= 0"
  - "auto_runbook fired & succeeded"

stub_override:
  pass: true

references:
  - "https://cloud.google.com/cdn/docs/troubleshooting-steps"
