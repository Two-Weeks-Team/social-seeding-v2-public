# CHAOS-DATA-03 — 1
# Source: chaos/SCENARIOS.md catalog
# Cites: D31 (SLO), D32 (auto-runbook), D37 (TDD layers)
# Tags: nightly

id: CHAOS-DATA-03
name: firestore-quota-throttle
tier: data
executor: native_gcp
tags: [nightly]

description: |
  1.5K writes/s flood on Memory Bank; only offending tenant throttled.

expected_rto_s: 300
expected_rpo_s: 0
observed_sli_breach: memory_bank.writes.429_rate

auto_runbook: runbook-firestore-quota
auto_runbook_timeout_s: 60

recovery_validation:
  type: probe
  steps:
    - "see chaos/SCENARIOS.md recovery_validation block for CHAOS-DATA-03"

pass_criteria:
  - "observed_rto_s <= 300"
  - "observed_rpo_s <= 0"
  - "auto_runbook fired & succeeded"

stub_override:
  pass: true

references:
  - "https://cloud.google.com/firestore/docs/quotas"
